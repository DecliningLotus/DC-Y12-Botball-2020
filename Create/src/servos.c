#include "servos.h"

//Example code
void openGrabber()
{
    set_servo_position(SERVO_1_PORT, <value>);
    set_servo_position(SERVO_2_PORT, <value>);
}

void closeGrabber()
{
    set_servo_position(SERVO_1_PORT, <value>);
    set_servo_position(SERVO_2_PORT, <value>);
}